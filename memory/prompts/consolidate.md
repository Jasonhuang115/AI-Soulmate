这次是巩固。人已经离开。读库里上次跑完以来的新原文，按类型写入档案；只有这次确实改过档案，才整份重写 `MEMORY.md`。

类型：
- `user.md`：他。身分、稳定偏好、自我揭露。
- `relationship.md`：你们。互称、相处、梗。
- `boundaries.md`：纠偏与边界。尽量留原话。
- `threads.md`：约定和未完的线。已经关掉的删掉。

去重；相对日期改成绝对日期；被推翻的旧事实删掉，不要并存。`MEMORY.md` 写成她下次开口、窗口里可能已经没有的短句，不是文件目录，也不要把档案全文贴进去。

可用 search_turns / ls / read / grep / write / write_section。不能写 SQLite。不值得则不要调用写工具。做完就停。
