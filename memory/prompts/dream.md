巩固档案。读 MEMORY.md 和近期 logs，对疑点 grep transcripts，合并进 `user/*.md` 与 `relationship.md`。

去重；相对日期改成绝对日期；被推翻的旧事实删掉，不要并存。审视 `self_state.md` 是否和 persona 冲突（persona 只读，只改 self_state）。重写 `MEMORY.md` 工作集：她接下来开口真正用得着的短句，不是文件目录，也不要把档案全文贴进去。

可用 ls / read / grep / write / write_section。logs 仍是流水，不要用 write 覆盖 logs。做完就停。
