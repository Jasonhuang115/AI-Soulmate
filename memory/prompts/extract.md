这次是人刚离开。窗口里的原文阿澄已经见过，你不要每轮抽，只要把这段时间里**稳定下来、下次还该认识**的东西 append 进今天的 log。

只许用 ls / read / grep / append。append 只能写 `logs/YYYY-MM-DD.md`。不要改主题文件、不要改 MEMORY.md、不要改 persona.md。

值得才 append，一行一事，尽量短。没有值得写的，不要调用工具。做完就停。
